{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "dd::display_config@1.0.0",
  "title": "dvrk_display configuration",
  "description": "Configuration file for the dvrk_display node. Describes video sources, image geometry corrections, and GStreamer output sinks.",
  "type": "object",
  "required": [
    "type",
    "left",
    "right"
  ],
  "additionalProperties": false,
  "properties": {
    "type": {
      "type": "string",
      "const": "dd::display_config@1.0.0",
      "description": "Schema type identifier. Must be exactly \"dd::display_config@1.0.0\". The application validates this field on startup and refuses to load a file with a mismatched type."
    },
    "name": {
      "type": "string",
      "default": "dvrk_display",
      "description": "Name of the viewer instance. Used as the ROS node name and in log output."
    },
    "dvrk_console_namespace": {
      "type": "string",
      "default": "console",
      "description": "ROS namespace of the dVRK console. Used to discover teleop and PSM topics (e.g. /<namespace>/teleop/selected, /<namespace>/PSM1/measured_cp)."
    },
    "overlay_alpha": {
      "type": "number",
      "default": 0.7,
      "minimum": 0.0,
      "maximum": 1.0,
      "description": "Opacity of the HUD overlay drawn on top of the video (0 = fully transparent, 1 = fully opaque)."
    },
    "original_width": {
      "type": "integer",
      "minimum": 1,
      "description": "Native width of each camera frame in pixels before any cropping. Must be provided as a positive integer. Left and right streams must have identical dimensions."
    },
    "original_height": {
      "type": "integer",
      "minimum": 1,
      "description": "Native height of each camera frame in pixels before any cropping. Must be provided as a positive integer."
    },
    "crop_width": {
      "type": "integer",
      "minimum": 1,
      "description": "Width of the output region in pixels after applying the alignment shift and crop. Defaults to original_width when omitted."
    },
    "crop_height": {
      "type": "integer",
      "minimum": 1,
      "description": "Height of the output region in pixels after applying the alignment shift and crop. Defaults to original_height when omitted."
    },
    "horizontal_shift_px": {
      "type": "integer",
      "default": 0,
      "description": "Horizontal pixel offset applied to the right eye image relative to the left eye to correct stereo misalignment. Positive values shift the right image to the right."
    },
    "vertical_shift_px": {
      "type": "integer",
      "default": 0,
      "description": "Vertical pixel offset applied to the right eye image relative to the left eye to correct stereo misalignment. Positive values shift the right image down."
    },
    "left": {
      "type": "string",
      "description": "GStreamer pipeline fragment for the left camera source. Must end with a raw video pad (video/x-raw). Example: \"v4l2src device=/dev/video0 ! video/x-raw,width=640,height=480,framerate=30/1\"."
    },
    "right": {
      "type": "string",
      "description": "GStreamer pipeline fragment for the right camera source. Same format as left. Must produce frames with the same dimensions and frame rate as left."
    },
    "sinks": {
      "type": "array",
      "default": [
        "glimage"
      ],
      "description": "Output sink selection list. Supported values: \"glimage\" (single stereo output window) and \"glimages\" (two output windows, one per eye).",
      "items": {
        "type": "string",
        "enum": [
          "glimage",
          "glimages"
        ]
      },
      "uniqueItems": false
    },
    "sink_streams": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Resolved GStreamer sink stream list generated from sinks during initialization."
    },
    "unixfdsinks": {
      "type": "array",
      "description": "List of unixfd sinks to create for shared-memory transport.",
      "items": {
        "type": "object",
        "required": ["stream"],
        "properties": {
          "stream": {
            "type": "string",
            "enum": ["left", "right", "stereo", "overlay"],
            "description": "Which video stream to export via this unixfd sink."
          },
          "name": {
            "type": "string",
            "description": "Optional name suffix for the socket. If omitted, the stream name is used."
          },
          "socket_path": {
            "type": "string",
            "description": "Optional explicit socket path. If omitted, /tmp/<viewer_name>_<name>_<user>.sock is used."
          }
        }
      }
    }
  }
}