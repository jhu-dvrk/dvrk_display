{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "dvrk_data:record@1.0.0",
  "title": "Video Record Configuration",
  "type": "object",
  "properties": {
    "type": {
      "type": "string",
      "const": "dvrk_data:record@1.0.0"
    },
    "configuration_files": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "List of additional configuration files to load and merge. Paths are resolved relative to the current config file's directory, with fallback to the master config file's directory. Circular references are detected and skipped."
    },
    "data_directory": {
      "type": "string",
      "description": "Directory where recorded sessions will be saved",
      "default": "."
    },
    "record_audio": {
      "type": "boolean",
      "description": "Whether to record audio from the default microphone. Merged via logical OR across configuration files.",
      "default": false
    },
    "ros_topics": {
      "type": "array",
      "items": {
        "type": "object",
        "required": ["name"],
        "properties": {
          "name": {
            "type": "string",
            "description": "The ROS 2 topic name"
          },
          "continuous": {
            "type": "boolean",
            "description": "If true, data for this topic is collected continuously once the first recording session is started by the user. If false, it is only collected when video recording is active.",
            "default": false
          }
        }
      },
      "description": "List of ROS 2 topics to record in a rosbag"
    },
    "stages": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Optional ordered list of stages to sequence recordings"
    },
    "tags": {
      "type": "array",
      "items": {
        "type": "string"
      },
      "description": "Optional list of event tag names that can be applied during recording"
    },
    "videos": {
      "type": "array",
      "items": {
        "type": "object",
        "required": [
          "name"
        ],
        "properties": {
          "name": {
            "type": "string",
            "description": "Display name of the video stream"
          },
          "stream": {
            "type": "string",
            "description": "GStreamer source pipeline description (e.g. 'v4l2src device=/dev/video0'). Ignored when unixfd_socket_path is defined."
          },
          "unixfd_socket_path": {
            "type": "string",
            "description": "If defined (even if empty), use unixfdsrc to subscribe to dvrk_display via UNIX socket. If empty, auto-generates socket path with username. If not defined, uses stream field instead."
          },
          "record": {
            "type": "boolean",
            "description": "Whether to record this stream to an MP4 file",
            "default": true
          },
          "timestamp_overlay": {
            "type": "boolean",
            "description": "Whether to burn GStreamer and wall-clock timestamps into the video preview",
            "default": false
          },
          "side_by_side": {
            "type": "string",
            "enum": ["LR", "RL"],
            "description": "Indicates the video contains a side-by-side stereo pair. 'LR' means left eye is on the left half, 'RL' means right eye is on the left half. This value is stored in the sidecar JSON and used by extract -S to split the video into separate left/right channels."
          },
          "encoding": {
            "type": "object",
            "description": "H.264 encoding parameters for the recorded MP4 file",
            "properties": {
              "bitrate": {
                "type": "integer",
                "description": "Bitrate in kbps",
                "default": 10000
              },
              "speed_preset": {
                "type": "integer",
                "description": "x264 speed preset (1-10). Lower is faster encoding, lower quality.",
                "default": 5
              },
              "key_int_max": {
                "type": "integer",
                "description": "Maximum keyframe interval",
                "default": 30
              },
              "frame_rate": {
                "type": "integer",
                "description": "Target frame rate for recording"
              },
              "width": {
                "type": "integer",
                "description": "Target width for recording"
              },
              "height": {
                "type": "integer",
                "description": "Target height for recording"
              }
            }
          }
        }
      }
    }
  },
  "required": [
    "type"
  ]
}
